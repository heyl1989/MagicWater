<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.heyl.magicwater">

    <application
        android:name=".App.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".BackgroudIntentService"
            android:exported="false">
        </service>

        <activity android:name=".SelectGradeActivity">
        </activity>
        <activity android:name=".SelectLevelActivity">
        </activity>
        <activity android:name=".GameActivity">
        </activity>
        <activity android:name=".GameActivity2">
        </activity>
        <activity android:name=".Drag">
        </activity>
    </application>

</manifest>